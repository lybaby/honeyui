import ElMenuSlider from '../menu-tea/src/menu-slider';

/* istanbul ignore next */
ElMenuSlider.install = function(Vue) {
  Vue.component(ElMenuSlider.name, ElMenuSlider);
};

export default ElMenuSlider;
